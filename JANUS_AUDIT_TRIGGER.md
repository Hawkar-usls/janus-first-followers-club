# JANUS AUDIT TRIGGER

This file triggers the Janus Audit and Fix workflow.

Expected checks:

- replace stale non-hyphen Pages URLs;
- replace stale non-hyphen GitHub repository URLs;
- validate JSON files;
- validate sitemap XML if present;
- verify Ritual of Two Portals exists in both `RITUAL_OF_TWO_PORTALS.md` and `index.html`.

Canonical Pages URL:

```text
https://hawkar-usls.github.io/janus-first-followers-club-/
```

Canonical repository URL:

```text
https://github.com/Hawkar-usls/janus-first-followers-club-
```
